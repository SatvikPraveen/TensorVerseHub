# Runtime dependencies for the full learning environment (notebooks + utilities).
# The library itself needs only what is declared in pyproject.toml; install it with
#   pip install -e ".[all]"
# This file pins a known-good notebook environment for `pip install -r requirements.txt`.

# Core
tensorflow>=2.16,<2.22
numpy>=1.26
matplotlib>=3.7
scikit-learn>=1.3

# Legacy Keras 2 stack for tensorflow-model-optimization (pruning / QAT):
#   export TF_USE_LEGACY_KERAS=1 before importing TensorFlow to use it.
tf-keras>=2.16
tensorflow-model-optimization>=0.8.0

# Export back-ends
tf2onnx>=1.16
onnx>=1.15
onnxruntime>=1.17

# Notebooks & data
jupyterlab>=4.0
ipywidgets>=8.0
pandas>=2.0
seaborn>=0.13
pillow>=10.0
tqdm>=4.66
tensorflow-datasets>=4.9
tensorflow-hub>=0.16
keras-tuner>=1.4
pyyaml>=6.0

# Serving examples
fastapi>=0.110
uvicorn[standard]>=0.29
flask>=3.0
streamlit>=1.32
